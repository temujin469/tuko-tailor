import { View, Text } from "react-native";
import React from "react";

const HandleWorker = ({ navigation }) => {
  return (
    <View>
      <Text>handleWorker</Text>
    </View>
  );
};

export default HandleWorker;
